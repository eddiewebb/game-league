<form method="post" action="{{ path('app_player_session_delete', {'id': player_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player_session.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
